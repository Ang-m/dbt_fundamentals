version: 2

sources:
    - name: stripe  
      database : raw1      
      schema: stripe      
      tables:
      
      - name: payment

    - name: jaffle_shop
      database: raw1
      schema: jaffle_shop
      tables:
        - name: customers
        - name: orders
